## 变更说明

<!-- 这个 PR 做了什么、为什么这么做 -->

## 验证

- [ ] `uv run pytest` 通过（或说明具体验证方式与结果）
- [ ] `uv run ruff check .` 与 `uv run pyright` 通过
- [ ] 涉及 UI 的改动已附截图 / 说明
- [ ] 文档已同步（README / CHANGELOG / docs，如适用）

## 回滚方式

<!-- 如何回退本变更（revert 提交 / 恢复 tag 等） -->
