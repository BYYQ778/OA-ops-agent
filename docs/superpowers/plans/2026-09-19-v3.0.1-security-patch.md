# v3.0.1 安全修复计划（2026-09-19）

> 来源：Codex 对 v3.0.0 的完整复审（6 条问题全部经 Hermes 逐条核验属实，另发现 3 处同类问题）。
> 结论口径：v3.0.0 可作为功能版，但按"默认安全"标准不应直接对外暴露；本计划完成 v3.0.1 闭环。
> 回退点：tag `pre-v3.0.1-security-fix`（c0ead77）。

## 用户已确认的决策

1. `network_diag.tls_verify` 默认 **true**（自签名内网站点需显式关，输出给指引）。
2. 会话 delete/rename 从 viewer 白名单移出 → 落 admin（与 SECURITY.md §三 对齐）；create 保留 viewer。
3. 附加发现 A：LocalInspector `_run_cmd` shell=True 一并修为 argv + shell=False。
4. PyPDF2 → pypdf 迁移（pyproject/lock/requirements/imports/测试）。
5. 完成后：同步绿色版 app\ → 重建绿色版 → 冒烟 → zip → 推送 + v3.0.1 Release（视网络）。

## 步骤与验证

| # | 内容 | 文件 | 验证 |
|---|---|---|---|
| 1 | 4 个 DB 执行器（MySQL/Redis/sqlcmd/sqlplus）改 argv + shell=False；LocalInspector 同改 | agents/db_inspector.py、agents/inspection_real.py | 对应测试更新 + 注入回归测试（分号 host 保持单一 argv）|
| 2 | 会话 delete/rename 移出 _VIEWER_POSTS | ui/middleware.py | test_auth_gate 补反向测试（viewer 403 / admin 放行）|
| 3 | http_health_check：URL/主机校验、元数据/链路本地硬阻断、重定向逐跳校验（≤3）、tls_verify 配置 | agents/network_diag.py、config.yaml | test_network_diag 更新 + 新增守卫用例 |
| 4 | SSH 默认 RejectPolicy + known_hosts；ssh_trust_unknown_hosts 开关（默认 false） | agents/inspection_real.py、config.yaml | test_inspection_real 新增用例 |
| 5 | 版本号统一 3.0.1（pyproject/main/server/desktop_app/Dockerfile/启动.bat/CHANGELOG）+ 交接文档头部 | 多处 | git grep 扫描无残留旧版本 |
| 6 | requirements.txt 声明遗留属性；PyPDF2→pypdf；uv.lock relock | requirements.txt、pyproject.toml、uv.lock、utils/doc_parser*.py | test_doc_parser 等 + uv lock --check（默认索引）|

## 收尾门槛（对齐 Codex 清单）

pytest 全量（927+新增全绿）/ ruff / pyright / compileall / git diff --check / 工作区干净。

## 文档与发布

- docs/SECURITY.md：§三 会话管理措辞、§七 SSRF 边界与 shell=False 落地、残余风险（CLI 密码进进程列表、DNS 重绑定窗口）。
- docs/维护日志.txt + docs/交接文档.txt + CHANGELOG.md（[3.0.1] Security）。
- 绿色版：数据备份 → PyInstaller 重建（~15min）→ app\ 同步 → zip 净版 → 冻结冒烟 → 恢复数据。
- GitHub：ssh-origin 推送 + tag v3.0.1 + Release（zip + SHA256SUMS，脚本适配 w6_release.py）。

## 范围纪律

只动上述文件/测试/文档；不重生成历史材料（generate_defense_doc 等）；分步 commit，不混无关改动。

## 执行结果（2026-09-19 完成，全部实测）

- 6 步修复 + 附加发现 A/B/C 全部完成；18 条安全新增用例，**945 passed**；Ruff / Pyright(0) /
  compileall / git diff --check / uv lock --check 全绿。
- 提交 6 个（be2602f→7c76dc7），推送 master（CI run 35451183472 双绿）+ tag v3.0.1
  （CI run 35451411089 双绿）。
- 绿色版重建（PACK_EXIT 0）→ app/ 同步 → zip 1.06GB/8234 文件（0 用户数据条目）→
  冻结冒烟（3.0.1 版本 / SSRF 实弹拦截 / PDF pypdf 解析 / RAG / RCA / 指标 / 首页）全通过。
- Release v3.0.1 附 zip + SHA256SUMS；详见维护日志与交接文档本日条目。
